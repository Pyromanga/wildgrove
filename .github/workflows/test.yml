name: Test-Trigger
on:
  push:
      branches: ["**"]
      jobs:
        test:
            runs-on: ubuntu-latest
                steps:
                      - run: echo "Workflow funktioniert!"name