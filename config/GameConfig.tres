class_name GameConfig extends Resource

# Das bleibt ein Dictionary, damit es flexibel ist
@export var valid_transitions: Dictionary = {
    "BOOT": ["MAIN_MENU", "LOADING"],
    "MAIN_MENU": ["LOADING", "CREDITS"],
    # ... hier trägst du ALLES ein, was vorher im Code stand
}